const dev = process.env.NODE_ENV !== "production";

export const server = dev ? "https://rotala.lv" : "https://rotala.lv";
// export const server = dev ? "http://localhost:3000" : "http://localhost:3000";
